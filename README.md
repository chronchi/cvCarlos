CV by [Awesome-CV](https://github.com/posquit0/Awesome-CV)
====

To compile the cv you only need to run the `cv.sh` file as following:
```bash
bash cv.sh
```
and the PDF will show. The PDF is saved in the `output_cv`
folder.
